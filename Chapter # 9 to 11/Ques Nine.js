// Question # 09

// Number Checker Game 

// Input from user
let numChecker = +prompt("Enter a Number")

// Check the condition
if (numChecker % 2 === 0) {
    alert(`${numChecker} is an even number.`);
} else {
    alert(`${numChecker} is an odd number.`);
}